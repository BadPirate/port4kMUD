# Procfile for Port4kMUD - Uses launch.sh script to manage startup

# Release phase - Run database migrations if needed (uncomment if using database migrations)
# release: cd server && yarn prisma migrate deploy

# Web process - Execute the launch script which handles both MUD and web app
web: ./launch.sh