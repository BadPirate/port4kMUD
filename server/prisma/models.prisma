// Core application models, after modifying rebuild schema with `yarn build:prisma-schemas`

// Example: Not used at this time, replace with your own model
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}